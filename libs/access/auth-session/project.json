{
  "name": "auth-session",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "libs/access/auth-session",
  "sourceRoot": "libs/access/auth-session/src",
  "prefix": "hub",
  "projectType": "library",
  "tags": ["type:shared", "scope:shared"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/out-tsc"],
      "options": {
        "command": "tsc -p libs/access/auth-session/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/access/auth-session/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
