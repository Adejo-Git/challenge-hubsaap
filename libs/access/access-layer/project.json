{
  "name": "access-layer",
  "projectType": "library",
  "root": "libs/access/access-layer",
  "sourceRoot": "libs/access/access-layer/src",
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{options.outputPath}"],
      "options": {
        "jestConfig": "libs/access/access-layer/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx eslint libs/access/access-layer/src --ext .ts"
      }
    }
  },
  "tags": ["type:access", "scope:shared"]
}
