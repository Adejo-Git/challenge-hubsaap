{
  "name": "access-layer-session",
  "projectType": "library",
  "root": "libs/access/access-layer/session",
  "sourceRoot": "libs/access/access-layer/session/src",
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{options.outputPath}"],
      "options": {
        "jestConfig": "libs/access/access-layer/session/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx eslint libs/access/access-layer/session/src --ext .ts"
      }
    }
  },
  "tags": ["type:access", "scope:shared"]
}
