/**
 * Public API — @hub/tool-registry
 *
 * Único ponto de entrada da lib tool-registry.
 * Consumidores devem importar exclusivamente por este barrel.
 * 
 * Exemplo:
 *   import { ToolRegistryService, ToolDescriptor } from '@hub/tool-registry';
 */
export * from './lib/index';
