/**
 * Entrypoint público da lib tool-data-access-sdk
 * 
 * Exporta:
 * - Clients tipados (WorkspaceClient, EvidenceClient, etc.)
 * - Models (DTOs, contratos)
 * - Errors (ToolDataError, ErrorMapper)
 * - Module (ToolDataAccessSdkModule)
 */

export * from './lib';
