import hubPreview from '../../tooling-storybook/.storybook/preview';

export const decorators = hubPreview.decorators;
export const parameters = hubPreview.parameters;

export default { decorators, parameters };
