// Root entrypoint for the error-model lib.
// Re-export the library public API from `src/index.ts`.
export * from './src/index';

// Keep this lightweight to satisfy build tools that expect a package root entrypoint.
